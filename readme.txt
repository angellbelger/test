This is just a glimpse to install the and run the qrcode-generator.

1 - Install python.
2 - Configure environment variables to be able to install libraries via the "pip install" command
3 - Install the libraries, Pillow and qrcode: pip install Pillow; pip install qrcode.

